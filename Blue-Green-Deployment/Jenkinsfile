pipeline {
  agent { label 'slave' }  // 👈 Your slave must have Docker installed

  environment {
    DOCKER_IMAGE = 'tameemdevops/blue-green'            // 🔁 DockerHub image name
    DOCKER_TAG = "Blue"                                 // 🔁 Can be "Green" for green deployment
    FULL_IMAGE = "${DOCKER_IMAGE}:${DOCKER_TAG}"
    DOCKER_REGISTRY = 'docker.io'
    DEPLOYMENT_NAME = 'mypythonapp-deploy'
    CONTAINER_NAME = 'myapp'
    NAMESPACE = 'default'
    MICROK8S_HOST = '137.184.65.39'
    REMOTE_DIR = '/root/bluegreenapp'
  }

  stages {
    stage('Checkout Code') {
      steps {
        git url: 'https://github.com/TameemZahid/Blue-Green_Deployment.git', branch: 'main'
      }
    }

    stage('Build Docker Image') {
      steps {
        sh '''
        
        echo "🛠️ Building Docker image: $FULL_IMAGE"
        cd Blue-Green-Deployment
        docker build -t $FULL_IMAGE .
        '''
      }
    }

    stage('Push to Docker Hub') {
      steps {
        withCredentials([usernamePassword(
          credentialsId: 'dev_cred',  // 🔐 Your Jenkins DockerHub creds
          usernameVariable: 'DOCKER_USER',
          passwordVariable: 'DOCKER_PASS'
        )]) {
          sh '''
          echo "🔐 Logging into Docker Hub..."
          echo $DOCKER_PASS | docker login -u $DOCKER_USER --password-stdin

          echo "📦 Pushing Docker image to Docker Hub: $FULL_IMAGE"
          docker push $FULL_IMAGE
          '''
        }
      }
    }

    stage('Deploy to MicroK8s') {
      steps {
        sshagent(['microk8']) {
          sh '''
          echo "🚀 Deploying to MicroK8s..."

          # Prepare remote directory
          ssh -o StrictHostKeyChecking=no root@$MICROK8S_HOST "mkdir -p $REMOTE_DIR"

          # Copy Kubernetes YAMLs
          scp kubernetes/deployment.yaml root@$MICROK8S_HOST:$REMOTE_DIR/deployment.yaml

          # Update deployment image and apply
          ssh -o StrictHostKeyChecking=no root@$MICROK8S_HOST "
            sed -i 's|image:.*|image: $FULL_IMAGE|' $REMOTE_DIR/deployment.yaml &&
            microk8s.kubectl apply -f $REMOTE_DIR/deployment.yaml &&
            microk8s.kubectl rollout status deployment/$DEPLOYMENT_NAME -n $NAMESPACE
          "
          '''
        }
      }
    }
  }

  post {
    success {
      echo "✅ Deployed Successfully: $FULL_IMAGE"
    }
    failure {
      echo "❌ Deployment Failed"
    }
  }
}
